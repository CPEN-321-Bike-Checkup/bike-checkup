import { StyleSheet } from 'react-native';

let CommonStyles = StyleSheet.create({
  editButtonText: {
    fontSize: 15,
    color: 'white',
    padding: 10,
    fontWeight: 'bold'
  }
});

export default CommonStyles;
